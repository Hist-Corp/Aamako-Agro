// The "Add product" screen reuses the same template editor.
// A new product's fields are saved under product-template.new.<field>
// until assigned a real slug by the Content Manager.
export { default } from '../[slug]/page';
